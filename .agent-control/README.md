# Agent Control Plane

This directory holds local runtime state for the Agent Memory Kit.

Tracked:

- `README.md`

Ignored:

- `agent-memory.sqlite*`
- `logs/`
- `secrets/`
- `tmp/`

Run `tools\init-agent-memory.ps1` in a target project to create the ignored runtime folders and admin token.
