# Windows PowerShell Extension

Owledge core is Python-first. The files under `extensions/windows-powershell/`
are optional convenience wrappers for users who explicitly want Windows script
wrappers around the Python tools.

Default setup, docs, generated kits, CI, plugins, and QA gates do not depend on
this extension.
