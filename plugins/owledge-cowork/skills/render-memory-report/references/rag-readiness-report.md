# RAG Readiness Report

Use for RAG, LightRAG, GraphRAG, vector DB ingestion, shared corpus, or retrieval readiness.

Sources: `indexes/*.jsonl`, `exports/rag/`, `exports/lightrag/`, `exports/graphrag/`, schemas, validation outputs.

Sections: readiness score, corpus composition, safety gates, broken edges, export status, ingestion commands, sources.

Visual pattern: score panels, corpus tables, pass/fail gates, and graph summary cards.
