ticket: V1M-10
status: accepted_independent_qa
base_commit: ec28349
branch: codex/v071-integration
scope: compact-daily-journey-candidate-claims-and-artifact-readiness
implementation:
  - "A fresh-process daily journey proves minimal install, explicit local user-global setup, reviewed research reuse across two allowlisted projects, and park/resurface behavior."
  - "Codex, Claude Code and generic MCP/CLI reuse the same project-local Core state without chat-history dependence; Generic remains exactly five tools."
  - "The explicit full Core profile ships its own validated Generic manifest at .owledge/runtime-conformance/generic-mcp-cli.json, so the V1M-10 Generic journey does not depend on a source-checkout add-on."
  - "The public entry docs and capability registry now identify the V1 Minimal Core and mark legacy Cowork/OpenCode/Hub surfaces outside the V1 reference-adapter/default path."
  - "Package metadata uses modern SPDX-style MIT license metadata to remove the upstream setuptools deprecation warning from the candidate build."
  - "The V1 source distribution and package-data declaration exclude optional add-ons, benchmark corpora, plugins, examples, nonessential docs, standalone bundles, tests and legacy Owlib so the GA artifact budget measures the Minimal Core rather than parked payload."
  - "A local standard-library artifact normalizer fixes wheel and Sdist metadata to a ZIP-safe epoch after the build, rejects unsafe or duplicate archive member names, and makes the release candidate byte-reproducible."
required_verification:
  - "Focused V1M-10 journey and bounded-claims suite passes."
  - "Public documentation, runtime adapter and V1M control-plane gates pass."
  - "A clean committed source builds wheel and sdist; artifact members and content have no private paths or internal dogfood payloads."
  - "Two clean local builds normalize to byte-identical artifacts: wheel SHA-256 8e36323a565a9ffd04cdd8635ed6c1bd074ebadf6f38f678e270c56b98a79a91 (311389 bytes); sdist SHA-256 8e634a58d2e918d4607462add8120e77fe29109cab0597d971a1b312be8a458e (250932 bytes)."
  - "The normalized wheel has 182 members and the normalized sdist 274; each contains only the nine declared Core Python modules, no private path members, and no forbidden add-on, benchmark, plugin, standalone, internal, test, Owlib or global-memory payload."
  - "A fresh offline CPython 3.13 environment installed the normalized wheel through uv pip --offline --no-index --no-deps; the imported tools package resolved from site-packages and full init, Generic five-tool bridge, and doctor passed. The normalized sdist rebuilt offline to a Core-only wheel."
negative_contracts:
  - "Ordinary recall excludes the parked feature while planning recall returns its reason and reconsideration trigger."
  - "The V1 docs do not claim publication, marketplace certification, remote sync, enterprise scope, or unexecuted macOS/Linux support."
known_limitations:
  - "The candidate remains local controlled Windows evidence; macOS and Linux wheel execution remains a mandatory publication prerequisite, not a V1 platform claim."
publication_authority: not_granted
independent_qa:
  result: accepted
  score: 95
  findings: []
next_exact_action: "Stop at G-V1M-GA and present the exact V1 candidate; do not start V1M-11 or perform external release actions."
