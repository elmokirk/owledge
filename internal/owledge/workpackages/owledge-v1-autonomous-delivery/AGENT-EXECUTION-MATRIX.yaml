schema_version: 2
document_version: 2
dependency_source: BACKLOG.yaml#tickets.depends_on
active_train: v1-minimal-core
default_profile: off
consent_modes: [off, phase_approval, per_ticket_approval]
historical_note: "Completed OW-071/080/081 work remains evidence only; unstarted legacy tickets are classified in BACKLOG.yaml#legacy_unstarted_disposition."
tickets:
  - {id: V1M-01, blockers: false, subagent: false, orchestrator: release-orchestrator, model_profile: frontier_orchestrator, qa_checker: true, red_team: true, approval_mode: per_ticket_approval, git_lane: integration, max_parallelism: 1}
  - {id: V1M-02, blockers: [V1M-01], subagent: true, orchestrator: release-orchestrator, model_profile: balanced_worker, qa_checker: true, red_team: false, approval_mode: phase_approval, git_lane: worker, max_parallelism: 1}
  - {id: V1M-03, blockers: [V1M-02], subagent: true, orchestrator: release-orchestrator, model_profile: balanced_worker, qa_checker: true, red_team: true, approval_mode: per_ticket_approval, git_lane: worker, max_parallelism: 1}
  - {id: V1M-04, blockers: [V1M-03], subagent: true, orchestrator: release-orchestrator, model_profile: frontier_orchestrator, qa_checker: true, red_team: true, approval_mode: per_ticket_approval, git_lane: worker, max_parallelism: 1}
  - {id: V1M-05, blockers: [V1M-04], subagent: true, orchestrator: release-orchestrator, model_profile: frontier_orchestrator, qa_checker: true, red_team: true, approval_mode: per_ticket_approval, git_lane: worker, max_parallelism: 1}
  - {id: V1M-06, blockers: [V1M-05], subagent: true, orchestrator: release-orchestrator, model_profile: frontier_orchestrator, qa_checker: true, red_team: true, approval_mode: per_ticket_approval, git_lane: worker, max_parallelism: 1}
  - {id: V1M-07, blockers: [V1M-06], subagent: true, orchestrator: release-orchestrator, model_profile: frontier_orchestrator, qa_checker: true, red_team: true, approval_mode: per_ticket_approval, git_lane: worker, max_parallelism: 1}
  - {id: V1M-08, blockers: [V1M-07], subagent: true, orchestrator: release-orchestrator, model_profile: balanced_worker, qa_checker: true, red_team: true, approval_mode: per_ticket_approval, git_lane: worker, max_parallelism: 1}
  - {id: V1M-09, blockers: [V1M-08], subagent: true, orchestrator: release-orchestrator, model_profile: frontier_orchestrator, qa_checker: true, red_team: true, approval_mode: per_ticket_approval, git_lane: worker, max_parallelism: 1}
  - {id: V1M-10, blockers: [V1M-09], subagent: false, orchestrator: release-orchestrator, model_profile: frontier_orchestrator, qa_checker: true, red_team: true, approval_mode: per_ticket_approval, git_lane: integration, max_parallelism: 1}
  - {id: V1M-11, blockers: [V1M-10], subagent: false, orchestrator: release-orchestrator, model_profile: balanced_worker, qa_checker: true, red_team: false, approval_mode: off, git_lane: integration, max_parallelism: 1}
