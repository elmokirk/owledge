---
memory_id: "mem:owledge:global:owledge:workpackage:v0.7.0-post-release-documentation-integrity"
doc_type: "workpackage"
status: "active"
visibility: "private"
data_class: "internal"
semantic_title: "v0.7.0 post-release documentation integrity checklist"
summary: "Evidence checklist for the v0.7.0 documentation and release-publication audit."
created_at: "2026-07-15T00:00:00Z"
updated_at: "2026-07-15T00:00:00Z"
---

# v0.7.0 Post-Release Documentation Integrity Checklist

## Resume State

Current resume point: Complete.

## Phase 1 — Establish release truth

- [x] Root version sources inspected: `VERSION`, `pyproject.toml`, README, and
  top CHANGELOG entry all identify 0.7.0.
- [x] Public README badge inspected: local tracked badge is `version-0.7.0`.
- [x] Active documentation inventory classified: v0.5/v0.6 references are
  historical, except the architecture note which is now explicitly labelled.
- [x] Tag and local branch compared: `v0.7.0` points to `346d2ff`; local
  `main` and its tracked `origin/main` both point to `72bf40f`. Live remote
  lookup is blocked by the sandbox network.

## Phase 2 — Correct and harden

- [x] Stale public release claim corrected: the v0.5 architecture note is
  labelled historical and points to current v0.7.0 guidance.
- [x] Regression coverage added: `release-trust` now compares `VERSION` with
  README badge, `pyproject.toml`, top CHANGELOG entry, and docs index.
- [x] Public documentation and release-trust gates pass (2026-07-15).

## Phase 3 — Publish verification

- [x] Publish-readiness passes: score 110, verdict `promote-candidate`.
- [x] Final branch/remote state verified: commit `9fdec5f` pushed to `main`.
- [x] Evidence and outcome recorded.

## Evidence

- `python tools/owledge.py test public-docs --project-root .`: passed.
- `python tools/owledge.py test release-trust --project-root .`: passed with
  17 checks, including the four version-consistency checks.
- `python tools/owledge.py test publish-readiness --project-root .` after the
  commit: passed, score 110, verdict `promote-candidate`.
- `python tools/owledge_core.py --project-root . doctor --mode kit`: passed,
  score 95; the expected manifest warning applies to the kit source checkout.
- `git push origin main`: pushed `9fdec5f` to `origin/main`.
