# Benchmark Results

Generated local benchmark summaries are written here by `benchmarks/run_benchmarks.py`.

`latest.json` and `latest.md` are ignored by git because benchmark claims should
be reproduced on the current machine and commit.
